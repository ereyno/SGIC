@model SGIC.Models.Viaje

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Viaje</legend>

    <div class="display-label">fecha</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.fecha)
    </div>

    <div class="display-label">numeroViaje</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.numeroViaje)
    </div>

    <div class="display-label">efectivo</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.efectivo)
    </div>

    <div class="display-label">importe</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.importe)
    </div>

    <div class="display-label">peaje</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.peaje)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
